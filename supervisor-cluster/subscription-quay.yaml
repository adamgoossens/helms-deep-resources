---
apiVersion: v1
kind: Namespace
metadata:
  name: helms-deep-registry-prod
spec: {}
---
apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: helms-deep-registry-prod
  namespace: helms-deep-registry-prod
spec:
  componentKinds:
  - group: apps.open-cluster-management.io
    kind: Subscription
  descriptor: {}
  selector:
    matchExpressions:
      - key: app
        operator: In
        values:
          - helms-deep-registry-prod
---
apiVersion: apps.open-cluster-management.io/v1
kind: Subscription
metadata:
  annotations:
    apps.open-cluster-management.io/git-branch: main
    apps.open-cluster-management.io/git-path: supervisor-cluster/quay
    apps.open-cluster-management.io/reconcile-option: merge
  labels:
    app: helms-deep-registry-prod
  name: helms-deep-registry-prod-subscription
  namespace: helms-deep-registry-prod
spec:
  channel: helms-deep-resources-ns/helms-deep-resources
  placement:
    placementRef:
      kind: PlacementRule
      name: helms-deep-registry-prod-placement-1
---
apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  labels:
    app: helms-deep-registry-prod
  name: helms-deep-registry-prod-placement-1
  namespace: helms-deep-registry-prod
spec:
  clusterSelector:
    matchLabels:
      'local-cluster': 'true'
