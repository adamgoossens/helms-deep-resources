apiVersion: redhatcop.redhat.io/v1alpha1
kind: GroupSync
metadata:
  name: keycloak-groupsync
  namespace: group-sync-operator
spec:
  providers:
    - keycloak:
        credentialsSecret:
          name: helms-deep-group-sync-credentials
          namespace: group-sync-operator
        insecure: true
        loginRealm: master
        realm: helms-deep
        scope: sub
        url: 'https://sso.apps.supervisor.adamgoossens.com'
      name: keycloak
  schedule: 0/15 * * * *
